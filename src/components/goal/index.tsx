import React from 'react';
import { Text } from 'react-native';

import { Container } from './styles';

const Goal: React.FC = () => {
    return (
        <Container>
            <Text>Goal</Text>
        </Container>
    );
};

export default Goal;
