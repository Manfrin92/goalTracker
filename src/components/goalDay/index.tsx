import React from 'react';
import { Text, TouchableOpacity, View } from 'react-native';

import { Day } from './styles';

const GoalDay: React.FC = () => {
    return <Day />;
};

export default GoalDay;
